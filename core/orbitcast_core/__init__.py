"""OrbitCast shared core package.

Holds pure, dependency-light logic reused by the API and the Dagster pipelines:
orbital propagation, H3 spatial fusion, and the deterministic reconfiguration
schedule (CLAUDE.md §4.1, §5, §7.1).
"""

__version__ = "0.1.0"
